# 40 Rename branches

- `git branch -m <oldname> <newname>` => -m for moving and in this case renaming
- if you are on the branch you want to rename `git branch -m newbranchname` and skip the source name as first argument and provide just target name
- the `-m` option stands for move